kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "kubis-rest"
spec:
  runPolicy: "Serial"
  source:
    git:
      uri: "https://github.com/Dimss/kubis-rest"
  strategy:
    sourceStrategy:
      from:
        kind: "ImageStreamTag"
        name: "openjdk18-openshift:latest"
  output:
    to:
      kind: "ImageStreamTag"
      name: "kubis-rest:0.1"
